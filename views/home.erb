<!DOCTYPE html>
<html>
<head>
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">	
	<title>my first sinatra app</title>
</head>
<body>
	<ul>
		<% @files.each do |file| %>
			<li><a href=<%="#{file}"%>><%= file %></a></li>
		<% end %>
	</ul>
	<% if params[:sort] == 'desc' %>
		<a href='/'>Sort Ascending Order</a>
	<% else %>
		<a href="/?sort=desc">Sort Decending Order</a>
	<% end %>

</body>
</html>